api_platform:
    title: Gym Manager
    version: 1.0.0
    mapping:
        paths: ['%kernel.project_dir%/src/Infrastructure/User/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]
        api_keys:
            apiKey:
                name: Authorization
                type: header
    defaults:
        stateless: true
        # Good cache defaults
        cache_headers:
            vary: [ 'Content-Type', 'Authorization', 'Origin' ]
